import 'package:flutter/material.dart';

class history_food_order_item extends StatelessWidget {
  const history_food_order_item({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
